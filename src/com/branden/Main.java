package com.branden;

/**
 * BA: main function moved from inside of SnakeGame
 */
public class Main {
    public static void main(String[] args) {
        // pass snake game to options so that the event listener can trigger the new game routine
        Options options = new Options();
    }
}
